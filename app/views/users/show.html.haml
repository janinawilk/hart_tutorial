- provide(:title, @user.name)

.row
  %aside.col-md-4
    = @user.name + ','
    = @user.email

  .col-md-8
    - if @user.microposts.any?
      %h3
        Microposts
      %ul.microposts
        = render @microposts
      = will_paginate @microposts
