= simple_form_for :session, url: login_path do |f|
  = f.input :email
  = f.input :password
  = link_to 'Forgot password?', new_password_reset_path
  = f.input :remember_me do
    = f.check_box :remember_me, require: false
  = f.button :submit, 'Log in'
